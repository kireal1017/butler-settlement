node_modules/
dist/
*.db
*.db-shm
*.db-wal
.DS_Store

# 등기부·점검 사진 원본 — 개인정보가 들어 있어 저장소에 남기지 않는다
server/uploads/

# 외부 연동 키 — .env.example 만 저장소에 둔다
.env
.env.local
!.env.example

# OMC 작업 상태 — 저장소에 둘 이유가 없다
.omc/
